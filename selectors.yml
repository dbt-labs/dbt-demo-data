# did you know you can do this?
# now you do!
# we exclude slow models from the default selector for this demo
selectors:
  - name: default
    default: true
    definition:
      method: fqn
      value: "*"
      exclude:
        - unfuzz_sql
      #  - pivot_py
      #  - cluster_py
